schema {
  query: Query
}

type Account {
  accountId: Int!
  username: String!
  email: String!
  trades: [Trade!]!
}

type Query {
  accountById(id: String!): Account!
}

type Trade {
  tradeId: Int!
  accountId: Int!
  symbol: String!
  amount: Int!
}